# Westeros Archive DBMS

## Description
This project is a simple DBMS for storing and manipulating data about the Seven Kingdoms of Westeros. It supports operations to create types, and to create, delete, and search for records.

## How to Run
1. Place the input file with operations in the same directory as `archive.py`.
2. Run the script with the input file path as an argument:

```sh
python3 archive.py path/to/input.txt
